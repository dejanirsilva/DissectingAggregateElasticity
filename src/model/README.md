# Model

Place model solution, simulation, and calibration code here.

Current subdirectories:

- `overleaf_imported/`: model code that was initially stored inside the Overleaf project and has been moved here
